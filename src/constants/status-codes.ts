export enum OKRStatusCode {
    ACTIVE = "active",
    COMPLETED = "completed",
    ON_HOLD = "on_hold",
    CANCELED = "canceled"
}

export enum TaskStatusCode {
    NEXT_UP = "next_up",
    IN_PROGRESS = "in_progress",
    WAITING_ON = "waiting_on",
    VALIDATING = "validating",
    COMPLETED = "completed",
    CANCELED = "canceled"
}

export enum ProjectStatusCode {
    PLANNING = "planning",
    ACTIVE = "active",
    ON_HOLD = "on_hold",
    COMPLETED = "completed",
    CANCELED = "canceled"
}